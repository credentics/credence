name: pass_doc_manager
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  get_it: ^8.2.0
  dio: ^5.9.0
  retrofit: ^4.7.2
  json_annotation: ^4.11.0
  path_provider: ^2.1.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.2.4
  google_mlkit_text_recognition: ^0.15.1
  image_picker: ^1.2.0
  desktop_drop: ^0.7.0
  flutter_svg: ^2.2.4
  file_selector: ^1.0.3
  # Used for picking large archives with withData:false so the file streams to a
  # cache path instead of being loaded into the Android Java heap (file_selector
  # OOMs on large vault archives). See _importVaultData.
  file_picker: ^10.3.10
  share_plus: ^10.1.4
  pdfrx: ^2.2.24
  archive: ^4.0.9
  flutter_local_notifications: ^20.1.0
  timezone: ^0.10.1
  cryptography: ^2.9.0
  local_auth: ^2.3.0
  url_launcher: ^6.3.2
  fleather: ^1.26.0
  home_widget: ^0.4.1
  markdown: ^7.3.0
  window_manager: ^0.4.3
  macos_ui: ^2.1.0
  appflowy_editor: ^6.2.0
  sqflite: ^2.4.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.5.4
  json_serializable: ^6.11.1
  # Used by the Hive test harness (test/support) to fake path_provider.
  path_provider_platform_interface: ^2.1.2
  plugin_platform_interface: ^2.1.8
  retrofit_generator: ^10.0.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/nav/
  fonts:
    - family: Manrope
      fonts:
        - asset: assets/google_fonts/Manrope-ExtraLight.ttf
          weight: 200
        - asset: assets/google_fonts/Manrope-Light.ttf
          weight: 300
        - asset: assets/google_fonts/Manrope-Regular.ttf
          weight: 400
        - asset: assets/google_fonts/Manrope-Medium.ttf
          weight: 500
        - asset: assets/google_fonts/Manrope-SemiBold.ttf
          weight: 600
        - asset: assets/google_fonts/Manrope-Bold.ttf
          weight: 700
        - asset: assets/google_fonts/Manrope-ExtraBold.ttf
          weight: 800
    - family: Inter
      fonts:
        - asset: assets/google_fonts/Inter_18pt-Thin.ttf
          weight: 100
        - asset: assets/google_fonts/Inter_18pt-ExtraLight.ttf
          weight: 200
        - asset: assets/google_fonts/Inter_18pt-Light.ttf
          weight: 300
        - asset: assets/google_fonts/Inter_18pt-Regular.ttf
          weight: 400
        - asset: assets/google_fonts/Inter_18pt-Medium.ttf
          weight: 500
        - asset: assets/google_fonts/Inter_18pt-SemiBold.ttf
          weight: 600
        - asset: assets/google_fonts/Inter_18pt-Bold.ttf
          weight: 700
        - asset: assets/google_fonts/Inter_18pt-ExtraBold.ttf
          weight: 800
        - asset: assets/google_fonts/Inter_18pt-Black.ttf
          weight: 900
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/plus_jakarta_sans/PlusJakartaSans-Variable.ttf
    - family: JetBrains Mono
      fonts:
        - asset: assets/google_fonts/JetBrainsMono-VariableFont_wght.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
